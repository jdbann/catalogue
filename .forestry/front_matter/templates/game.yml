---
label: Game
hide_body: true
fields:
- name: name
  type: text
  config:
    required: true
  label: Name
- name: image
  type: file
  config:
    maxSize: 64
  label: Image
  description: A cover image or example of the game in progress.
- name: description
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: true
    schema:
      format: markdown
  label: Description
